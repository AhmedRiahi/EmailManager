package com.email.manager.renderer;

import com.email.manager.models.EmailTemplate;
import com.email.manager.models.markup.MarkupValues;

public class EmailRenderer {

    public static void render(EmailTemplate emailTemplate, MarkupValues markupValues){

    }
}
